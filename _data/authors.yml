# Per-post author overrides (optional).
#
# Only used if a post/page sets `author: <key>` in its front matter; otherwise
# author info falls back to `author:` in _config.yml. Not currently used —
# add entries here only if a page needs a different author than the site default.
